name: CI_build

on: [push, pull_request]

jobs:
  build:

    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        build_configuration: [Release, Debug]
        build_platform: [x64, x86]

    steps:
    - name: Checkout repo
      uses: actions/checkout@v6

    - name: Add msbuild to PATH
      uses: microsoft/setup-msbuild@v3

    - name: MSBuild of plugin dll
      working-directory: .\
      run: msbuild DraftSync.csproj /m /p:configuration="${{ matrix.build_configuration }}" /p:platform="${{ matrix.build_platform }}"

    - uses: olegtarasov/get-tag@v2.1.4
      id: tagName
      
    - name: zip artifacts for x64
      if: matrix.build_platform == 'x64' && matrix.build_configuration == 'Release'
      run: 7z a DraftSync_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip .\bin\${{ matrix.build_configuration }}-x64\DraftSync.dll

    - name: zip artifacts for x86
      if: matrix.build_platform == 'x86' && matrix.build_configuration == 'Release'
      run: 7z a DraftSync_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip .\bin\${{ matrix.build_configuration }}\DraftSync.dll

    - name: Archive artifacts for ${{ matrix.build_platform }}
      if: matrix.build_configuration == 'Release'
      uses: actions/upload-artifact@v7
      with:
          name: DraftSync_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip
          path: DraftSync_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip

    - name: Create release on tagging
      uses: softprops/action-gh-release@v3
      if: startsWith(github.ref, 'refs/tags/')
      with:
          files: DraftSync_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip